class Company
types

values

instance variables

	private name : Utils`String := [];
	
operations

	-- constructor of the company class 
		public Company :  Utils`String ==> Company
	  	Company (companyName) == (
	  		name := companyName;
	  		return self
	  	)
	  	pre len companyName > 0;
	  	
	 -- returns the company's name
	 pure public GetName : () ==> Utils`String
	  	GetName () == (
	  		return name;
	  	);
	  	
	  -- sets the company's name
	 public SetName : Utils`String ==> ()
	  	SetName (n) == (
	  		name := n;
	  	);
	  	
functions

traces

end Company