class Test
types

values
 
instance variables

-- TESTES

 	attendee : Influential := new Influential("Andreia", "CEO", new Company("MFES"), "Sou uma pessoa fixe", "Portugal");
	w : WebSummit := new WebSummit();
	
	d : Utils`Date := mk_Utils`Date(2001,1,1);
	t1 : Utils`Time := mk_Utils`Time(15,40);
	t2 : Utils`Time := mk_Utils`Time(16,40);
 	talk1 : Talk := new Talk("Talk1", "É a talk 1", d, t1, 40);
  talk2 : Talk := new Talk("Talk2", "É a talk 2", d, t2, 40);
 	c : Conference := new Conference("Conf 1", "Conf 1 detail");
 	
operations
	 public static main: () ==> ()
    main() ==
    (
        new Test().run();
    );
   
    public run: () ==> ()
    run() ==
    (
        c.AddTalk(talk1);
        c.AddTalk(talk2);
        
    );
functions
  
traces
-- TODO Define Combinatorial Test Traces here
end Test