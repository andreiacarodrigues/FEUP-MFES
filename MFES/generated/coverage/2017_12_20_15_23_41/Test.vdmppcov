class Test
types

values
 
instance variables

-- TESTES
 	attendee : Influential := new Influential("Andreia", "CEO", new Company("MFES"), "Sou uma pessoa fixe", "Portugal");
	d : Utils`Date := mk_Utils`Date(2001,1,1);
	t : Utils`Time := mk_Utils`Time(15,40);
 	talk : Talk := new Talk("Talk1", "É a talk 1", d, t, 40);
 	
 	s : Influential := talk.AddSpeaker(attendee);
 	s1 : Influential := talk.AddAttendee(attendee);
 	
 	
operations
	 public static main: () ==> ()
    main() ==
    (
        new Test().run();
    );
   
    public run: () ==> ()
    run() ==
    (
        attendee.Print();
    );
functions
  
traces
-- TODO Define Combinatorial Test Traces here
end Test